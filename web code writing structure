// js/css import

write all js name ans css name in xml file
read that xml file through any php function 
all php functions should be written in one sepreate file

after reading xml file , make uri which point to some php file with some parameter
that php file read all the file name mentioned and compressed data and write them in to one file.

for js- one js file and for css one css file
gzdeflate - to compress string

// use curl to read remote url

// javacsript caring

var cars = ["Saab", "Volvo", "BMW"] - array
var person = {firstName:"John", lastName:"Doe", age:50, eyeColor:"blue"} - object
var employees = [
    {"firstName":"John", "lastName":"Doe"},
    {"firstName":"Anna", "lastName":"Smith"},
    {"firstName":"Peter", "lastName": "Jones"}
] - json

retrieveby - employees[0].firstName

//
